<ul>
  <% @files.each do |file| %>
  <li>
    <a href="/<%= file %>"><%= file %></a>
    <a href="/<%= file %>/edit"> edit file</a>
    <form class="inline" action="/<%= file %>/delete" method="post"> 
      <button type="submit">delete</button>
    </form>
    <form class="inline" action="/<%= file %>/duplicate" method="post"> 
      <button type="submit">duplicate</button>
    </form>
    <a href="/<%= file %>/past_versions"> View Previous Versions</a>
  </li>
  <% end %>
</ul

  <p><a href="/new">New Document</a></p>
  <% if user_signed_in? %>
    <p class="user-status"> Signed in as <%= session[:username] %></p>
    <form action="/users/signout" method="post">       
      <button type="submit">Sign Out</button>
    </form>
  <% else %>
  <p class="user-status"><a href="/users/signin">Sign In</a></p>
<% end %>


<%= @valid_users %>